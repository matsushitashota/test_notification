name: Notify Slack for Old PRs

on:
  schedule:
    - cron: "40 7 * * *" # 毎日午後4時40分に実行

jobs:
  notify-slack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Send Hello World to Slack
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          text: "Hello World"
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
